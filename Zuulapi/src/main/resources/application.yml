spring:
  application:
    name: zuulapi
server:
  port: 10001
management:
  port: 10002
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:10000/eureka
  instance:
    lease-renewal-interval-in-seconds: 5