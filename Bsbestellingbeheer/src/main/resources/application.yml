server:
  port: 10007
eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:10000/eureka
  instance:
    lease-renewal-interval-in-seconds: 5
spring:
  application:
    name: bsbestellingenbeheer
  data:
    mongodb:
      port: 27017
